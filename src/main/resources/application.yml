spring:
  datasource:
    #mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/chess?serverTimezone=Asia/Seoul
    username: root
    password: 1111

    #h2
#    url: jdbc:h2:tcp://localhost/~/chat
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        # show_sql: true
#        format_sql: true (수정)

logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace

jwt:
  secret: hello.world.my.name.is.bae.

